/* eslint-disable linebreak-style */
/* eslint-disable class-methods-use-this */

export default class Genre {
  constructor(data) {
    const {
      id,
      name,
    } = data;

    this.id = id;
    this.name = name;
  }
}
